namespace Snake
{
    //TODO: Based on the members, this class doesn't represent a circle. I suppose it's a point.
    //TODO: Always write access modifiers (... class Circle).
    class Circle
    {
        public int X { get; set; }
        public int Y { get; set; }

        public Circle()
        {
            X = 0;
            Y = 0;
        }
    }
}
